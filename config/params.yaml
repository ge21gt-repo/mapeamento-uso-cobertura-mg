model_type: "unet"            # "deeplab" ou "segformer" ou "unet"
image_dir: "dataset/Augmented/Images"
mask_dir: "dataset/Augmented/Masks"

num_classes: 16
batch_size: 2
num_epochs: 1
learning_rate: 0.0001
img_size: 128

device: "cuda"
output_dir: "./output"
patience: 20
save_steps: 500
